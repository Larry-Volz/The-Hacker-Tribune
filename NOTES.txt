NOTES:

TODO:
- favorites being added - but ALL stars need to show up/stay filled in
  --> change that with a check in generateHTML() in ui.js
- and when removed/fills in... screwed up - check it out
(currentUser.addFavorites()/removeFavorites()?)
- $favoritedAricles not used?
- 
